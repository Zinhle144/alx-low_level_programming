#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: the character to check
 * Return: 1 if b is lowercase, 0 otherwise
 */
int _islower(int b)
{
	return (b >= 'a' && b <= 'z');
}
